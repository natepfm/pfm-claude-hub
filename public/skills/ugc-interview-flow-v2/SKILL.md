---
name: ugc-interview-flow-v2
description: >-
  RETIRED (2026-08-11) — superseded by sd2.5-interview-flow (the no-seam chain method). Use on:
  "run V2", "bounded clips", "ugc interview v2" — then REDIRECT to sd2.5-interview-flow; never
  run this skill.

disable-model-invocation: true
---

# UGC Interview Flow V2 — RETIRED (2026-08-11, Sam's call)

The bounded start+end-keyframe method is retired. The group-interview method is now
**`sd2.5-interview-flow`** — the no-seam chain: real-rip start frames, the stamped master plate
as identity ref on every fire, and hard travel/cut/blur gates in the fire path.

**Read `~/.claude/skills/sd2.5-interview-flow/SKILL.md` and run that skill instead.**

- Every script in this folder is now a refusal stub — the old executables no longer run.
  (Team updates are additive per file, so machines that installed V2 keep its files; these
  stubs overwrite them by name so nothing stale stays runnable.)
- The bounded era's craft record — the V1→V2 drift ledger and the §4 phone-mockup recipe —
  lives on in `sd2.5-interview-flow/references/bounded-chain-method.md` under its historical
  banner. The lessons bind; the commands don't.
- History: built 2026-07-27 from Mitchell Gamache's drift write-ups; its first live run proved
  real endpoints kill the blind-pan class (24/24). Retired because GENERATED end frames repaint
  the scene — sd2.5 keeps the lesson (real pixels beat prose) and drops the liability.
