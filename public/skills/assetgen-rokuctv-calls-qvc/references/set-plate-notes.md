# SaveMaxAuto Live set plate — prop cluster + asset-gen laws

Moved from SKILL.md Step 1 (2026-07-09). The environment-master detail for the QVC set.

**Environment master (set plate)** — the EMPTY SaveMaxAuto Live set as its own locked master, gen'd + approved BEFORE any people go in. **Model: `gpt_image_2`, 16:9, 2k** (exact replication + text, not NB). Bright blue home-shopping studio (wavy backlit wall, metal grid, warm light strip, center lightbox), white glossy two-tier desk, white flocked winter tree + gifts, wood floor.

**Locked branded PROP CLUSTER on the desk (Houston, 07-09 — replaced the old laptops-on-desk staging Dima scrapped):** center — a navy A-frame easel **sign carrying the SaveMaxAuto logo**; the **Max TRIO** — fluffy golden-retriever plush (upper-left) + bobblehead on a round base (upper-right) + a carved matte-finish **statue** on the lower shelf; corded black desk phone (front-left); dark-blue toy sedan (lower shelf center, logo-free = brand-clean); two white **paw-logo mugs** (lower shelf right); three white boxes sitting FLAT ON the lower shelf (not floating in front); a shiny **red "take a call" buzzer** on the upper-RIGHT (the host presses it to take a caller).

**🔴 Three asset-gen laws this maiden cost hours to learn (all now in [[feedback_match_example_reference_first]] + qvc-screen-graphics skill):**
1. **Reference-first** — recreate off the prior/original plate passed as ref 1, never describe the room from scratch.
2. **The SMA logo is the REAL asset passed as a 2nd `--image` ref, never prose** — it is navy "SaveMax" + bright-blue italic "Auto" (`2. Client Media Assets/SMA - Brand Folder/Logos/savemaxauto-wordmark.png` = white/cyan for the navy sign), NOT gold text.
3. **gpt edits accumulate HAZE — never stack edit-on-edit; re-edit from the SHARPEST base in ONE pass** with "sharp, high-clarity, no haze/overlay" in the prompt.

Approved plate → library + Prompts sheet like a character master. Houston locked plate + winning prompt: `Elements/Footage/Reference/SaveMaxAuto Live Set/SMA_Live_Set_Plate_APPROVED_PropCluster.png` + `Elements/Prompts/SMA_Live_Set_Plate_prompt_APPROVED.txt`.
