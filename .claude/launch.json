{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "hub-dev",
      "runtimeExecutable": "npm",
      "runtimeArgs": ["run", "dev"],
      "port": 3000,
      "//": "autoPort must stay false: http://localhost:3000/api/auth/callback/google is the redirect URI registered in Google Cloud, so local sign-in only works on 3000.",
      "autoPort": false
    }
  ]
}
